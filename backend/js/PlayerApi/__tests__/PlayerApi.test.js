const PlayerApi = require('..');

describe('player api tests', () => {
  let api;

  beforeEach(() => {
    api = new PlayerApi();
  });

  describe('setup', () => {
    test('create new PlayerApi', () => {

    });
  });

  describe('join / leave games', () => {

  });

  describe('handle actions', () => {
    test('handle close', () => {

    });

    describe('handle message', () => {
      test('create new PlayerApi', () => {

      });
    });
  });
});
